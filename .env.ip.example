# A static public IPv4 address. Do not add http://, https://, or a port.
APP_IP=203.0.113.10
# Required while Compose reads the base production file; the IP override also
# sets it to this value inside the Caddy container.
APP_ORIGIN=https://203.0.113.10
ACME_EMAIL=ops@example.com

POSTGRES_DB=smart_qc_capture_system
POSTGRES_USER=qc_user
POSTGRES_PASSWORD=CHANGE_ME_TO_A_LONG_RANDOM_PASSWORD
